export enum DriverTripStatus {
  OFFERED = 'offered',
  ACCEPTED = 'accepted',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  DECLINED = 'declined',
}
